apiVersion: v1
kind: Pod
metadata:
  name: __NOME__
  labels:
    app: trabalho-escalonador
spec:
__PLACEMENT__
  containers:
  - name: tarefa
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    resources:
      requests:
        cpu: "10m"
        memory: "16Mi"
